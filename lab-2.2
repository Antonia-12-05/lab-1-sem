#include <stdio.h>

int main()
{
    const int niters = 1000;

    double pi = 0.0, sign = 1.0;
    for (int i = 0; i < niters; ++i)
    {
        pi += sign / (double)(1 + i * 2);
        sign = -sign;
    }
    pi *= 4;

    printf("pi = %.12lf\n", pi);
    getchar();

    return 0;
}
