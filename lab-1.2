#include <stdio.h>
#include <math.h>

int main()
{
    int a = 0, a1 = 0, a2 = 0, a3 = 0, a4 = 0, a5 = 0, a6 = 0;
    printf("enter numbur\n");

    scanf_s("%d", &a); //вводим номер билета
    a6 = a % 10; //делим число на 10, значение остатка присваиваем переменной a6
    a5 = a / 10 % 10;
    a4 = a / 100 % 10;
    a3 = a / 1000 % 10;
    a2 = a / 10000 % 10;
    a1 = a / 100000 % 10;

    if ((a1 + a2 + a3) == (a4 + a5 + a6)) // если сумма трех первых цифр равна сумме трех его последних цифр
    {
        printf("lucky\n"); //выводим, что билет счастливый
    }
    else
        printf("not lucky\n"); //если нет, выводим, что билет не счастливый
    return 0;
}
